{
  "name": "uncharted",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "An educational graph-layout solver lab (single-file build).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.1.0",
    "vitest": "^2.1.8"
  }
}
